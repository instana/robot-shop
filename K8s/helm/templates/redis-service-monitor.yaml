apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    release: kube-prometheus-stack
  name: redis
spec:
  endpoints:
    - path: /metrics
      targetPort: 9121
  namespaceSelector:
    any: true
  selector:
    matchLabels:
      service: redis