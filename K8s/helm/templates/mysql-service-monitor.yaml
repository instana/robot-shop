apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    release: kube-prometheus-stack
  name: mysql
spec:
  endpoints:
    - path: /metrics
      targetPort: 9104
  namespaceSelector:
    any: true
  selector:
    matchLabels:
      service: mysql