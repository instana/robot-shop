apiVersion: v1
kind: Service
metadata:
  labels:
    service: dispatch
  name: dispatch
spec:
  clusterIP: None
  ports:
  - name: headless
    port: 55555
    targetPort: 0
  selector:
    service: dispatch
